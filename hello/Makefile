# Каталог с исходными кодами ядра. По существующим соглашениям нужно сделать ссылку на соответствующее дерево исходных кодов из каталога с скомпилированными модулями
KDIR := /lib/modules/$(shell uname -r)/build
# Текущий рабочий каталог, в котором расположены исходные коды модулей

PWD := $(shell pwd)

# Список модулей для сборки
obj-m := hello.o

all:
	$(MAKE) -C $(KDIR) M=$(PWD) modules


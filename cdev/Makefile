# Каталог с исходными кодами ядра.
# Нужно указать путь к каталогу с исходными кодами ядра, которые соответствуют работающему на компьютере ядру.
KDIR := /lib/modules/$(shell uname -r)/build

# Текущий рабочий каталог, в котором расположены исходные коды модулей
PWD := $(shell pwd)

# Список модулей для сборки
obj-m := mephi_cdev.o

all:
	$(MAKE) -C $(KDIR) M=$(PWD) modules

